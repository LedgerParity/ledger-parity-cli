# Contributing to ledger-parity-cli

Thank you for contributing to `ledger-parity-cli`!

## Guidelines
- Follow standard Go formatting (`gofmt`).
- Keep CLI dependencies lightweight.
- Verify CLI outputs with `./ledger-parity --demo`.
